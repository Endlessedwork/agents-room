---
phase: 11-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/rooms/ConversationPanel.tsx
  - src/app/api/rooms/[roomId]/route.ts
autonomous: true
requirements: [DEBT-01, DEBT-03]

must_haves:
  truths:
    - "ConversationPanel.tsx no longer exists in the codebase"
    - "npm run build passes cleanly after deletion"
    - "GET /api/rooms/:roomId response does not contain a messages array"
    - "Room detail page still loads correctly (roomAgents still included)"
  artifacts:
    - path: "src/app/api/rooms/[roomId]/route.ts"
      provides: "Room detail endpoint without messages over-fetching"
      contains: "roomAgents: true"
  key_links:
    - from: "src/app/(dashboard)/rooms/[roomId]/page.tsx"
      to: "/api/rooms/:roomId"
      via: "fetch for room detail"
      pattern: "roomAgents"
---

<objective>
Remove dead code and narrow the room detail endpoint to eliminate over-fetching.

Purpose: Clean up orphaned files and stop returning unused data from the API.
Output: ConversationPanel.tsx deleted, room detail GET no longer returns messages relation.
</objective>

<execution_context>
@/home/vsman/agents-room/.claude/get-shit-done/workflows/execute-plan.md
@/home/vsman/agents-room/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tech-debt-cleanup/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned ConversationPanel.tsx and remove messages from room detail endpoint</name>
  <files>src/components/rooms/ConversationPanel.tsx, src/app/api/rooms/[roomId]/route.ts</files>
  <read_first>
    - src/components/rooms/ConversationPanel.tsx (confirm file exists before deletion)
    - src/app/api/rooms/[roomId]/route.ts (see current GET handler with `messages: true` in with clause)
    - src/app/(dashboard)/rooms/[roomId]/page.tsx (confirm consumer does not use messages from room detail response)
  </read_first>
  <action>
1. Delete `src/components/rooms/ConversationPanel.tsx` entirely (rm the file).

2. In `src/app/api/rooms/[roomId]/route.ts`, modify the GET handler's Drizzle query. Change:
```typescript
with: {
  roomAgents: true,
  messages: true,
},
```
To:
```typescript
with: {
  roomAgents: true,
},
```
Only the GET handler uses this `with` clause. DELETE returns `{ ok: true }` and PATCH returns from `.returning()` — neither loads relations. Do not modify DELETE or PATCH handlers.

3. Run `npm run build` to confirm the build passes with ConversationPanel.tsx deleted.
4. Run `npm test` to confirm no test regressions.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/rooms/ConversationPanel.tsx` returns "No such file or directory"
    - `grep -n "messages: true" src/app/api/rooms/[roomId]/route.ts` returns no matches
    - `grep -n "roomAgents: true" src/app/api/rooms/[roomId]/route.ts` returns exactly one match
    - `npm run build` exits with code 0
    - `npm test` exits with code 0
  </acceptance_criteria>
  <done>ConversationPanel.tsx is deleted, room detail endpoint no longer returns messages relation, build and tests pass</done>
</task>

</tasks>

<verification>
- `npm run build` passes (DEBT-01 gate)
- `grep -rn "ConversationPanel" src/` returns zero results
- `grep -n "messages: true" src/app/api/rooms/[roomId]/route.ts` returns zero results
- Room detail API response inspectable via: `curl localhost:3000/api/rooms/:id | jq keys` should not include "messages"
</verification>

<success_criteria>
- ConversationPanel.tsx does not exist in the codebase
- Build passes without the deleted file
- Room detail GET endpoint returns roomAgents but not messages
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-tech-debt-cleanup/11-01-SUMMARY.md`
</output>
