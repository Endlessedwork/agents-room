---
phase: 11-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/llm/gateway.test.ts
  - tests/conversation/manager.test.ts
  - tests/conversation/manager-sse.test.ts
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "npx tsc --noEmit reports zero errors across all files"
    - "No `as any` casts added to production code — only test mock boundaries"
    - "All existing tests still pass (npm test)"
  artifacts:
    - path: "tests/llm/gateway.test.ts"
      provides: "Type-correct LanguageModel mock assertions"
      contains: "(model as any).doGenerate"
    - path: "tests/conversation/manager.test.ts"
      provides: "Type-correct StreamTextResult mocks via makeMockStream"
      contains: "as unknown as ReturnType<typeof streamLLM>"
    - path: "tests/conversation/manager-sse.test.ts"
      provides: "Type-correct StreamTextResult mocks via makeMockStream"
      contains: "as unknown as ReturnType<typeof streamLLM>"
  key_links:
    - from: "tests/conversation/manager.test.ts"
      to: "src/lib/llm/gateway.ts"
      via: "vi.mock return type cast"
      pattern: "ReturnType<typeof streamLLM>"
    - from: "tests/conversation/manager-sse.test.ts"
      to: "src/lib/llm/gateway.ts"
      via: "vi.mock return type cast"
      pattern: "ReturnType<typeof streamLLM>"
---

<objective>
Fix all TypeScript errors in test files so `npx tsc --noEmit` passes with zero errors.

Purpose: Eliminate type drift between test mocks and Vercel AI SDK v6 types without modifying production code.
Output: Three test files updated with correct type casts at mock boundaries.
</objective>

<execution_context>
@/home/vsman/agents-room/.claude/get-shit-done/workflows/execute-plan.md
@/home/vsman/agents-room/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tech-debt-cleanup/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LanguageModel type errors in gateway.test.ts</name>
  <files>tests/llm/gateway.test.ts</files>
  <read_first>
    - tests/llm/gateway.test.ts (see current model.doGenerate assertions at lines 98, 104, 112, 122, 128)
    - src/lib/llm/providers.ts (see getModel return type)
  </read_first>
  <action>
In `tests/llm/gateway.test.ts`, the `LanguageModel` type in AI SDK v6 is now a union type `GlobalProviderModelId | LanguageModelV3 | LanguageModelV2`. Accessing `model.doGenerate` fails because TypeScript sees the string branch of the union which has no `doGenerate` property.

Fix all 5 occurrences of `model.doGenerate` by casting to `any` at the access site. Change each occurrence from:
```typescript
expect(model.doGenerate).toBeDefined();
```
To:
```typescript
expect((model as any).doGenerate).toBeDefined();
```

Lines to change: 98, 104, 112, 122, 128.

Do NOT cast the `model` variable itself or the `getModel` function — only cast at the assertion access point.

Run `npx tsc --noEmit --pretty tests/llm/gateway.test.ts 2>&1 | head -20` to confirm zero errors in this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "gateway.test.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "(model as any).doGenerate" tests/llm/gateway.test.ts` returns 5
    - `grep -c "model\.doGenerate" tests/llm/gateway.test.ts` returns 5 (same lines, matched by the broader pattern)
    - `npx tsc --noEmit 2>&1 | grep "gateway.test.ts"` returns zero lines
    - `npx vitest run tests/llm/gateway.test.ts` passes
  </acceptance_criteria>
  <done>All 5 LanguageModel.doGenerate assertions use `(model as any).doGenerate`, tsc reports zero errors for gateway.test.ts</done>
</task>

<task type="auto">
  <name>Task 2: Fix StreamTextResult and agent.name type errors in manager.test.ts and manager-sse.test.ts</name>
  <files>tests/conversation/manager.test.ts, tests/conversation/manager-sse.test.ts</files>
  <read_first>
    - tests/conversation/manager.test.ts (see makeMockStream at line 75, inline mocks throughout, agent.name at line 461)
    - tests/conversation/manager-sse.test.ts (see makeMockStream at line 36, inline mocks at lines 201 and 226)
    - src/lib/llm/gateway.ts (see streamLLM return type — it returns `streamText(...)` which is `StreamTextResult`)
  </read_first>
  <action>
Three categories of fixes across both files. All fixes are type casts at mock boundaries only — zero production code changes.

**Fix A: makeMockStream() return type cast**

In `tests/conversation/manager.test.ts` (line 75), add import and cast:
1. Add import at top of file: `import type { streamLLM } from '@/lib/llm/gateway';`
2. Change `makeMockStream` return statement. The function currently returns a bare object `{ textStream, usage }`. Add `as unknown as ReturnType<typeof streamLLM>` to the return:
```typescript
function makeMockStream(yields: string[] = ['mock ', 'response']) {
  return {
    textStream: (async function* () {
      for (const chunk of yields) {
        yield chunk;
      }
    })(),
    usage: Promise.resolve({ inputTokens: 10, outputTokens: 5 }),
  } as unknown as ReturnType<typeof streamLLM>;
}
```

In `tests/conversation/manager-sse.test.ts` (line 36), same fix:
1. Add import at top: `import type { streamLLM } from '@/lib/llm/gateway';`
2. Cast `makeMockStream` return:
```typescript
function makeMockStream(chunks: string[] = ['hello ', 'world']) {
  return {
    textStream: (async function* () {
      for (const chunk of chunks) {
        yield chunk;
      }
    })(),
    usage: Promise.resolve({ inputTokens: 10, outputTokens: 5 }),
  } as unknown as ReturnType<typeof streamLLM>;
}
```

**Fix B: Inline mock return type casts**

In `tests/conversation/manager.test.ts`, there are inline mocks that return `{ textStream, usage }` without going through `makeMockStream`. These are at lines where `mockStreamLLM.mockImplementation` returns an inline object. Find each inline `return { textStream: ..., usage: ... }` and the bare `{ textStream: ..., usage: ... }` in arrow returns inside `mockImplementation`. Add `as unknown as ReturnType<typeof streamLLM>` to each inline return.

Specific locations in manager.test.ts:
- Line ~195 (stop aborts in-flight stream test): the `return { textStream:..., usage:... }` inside mockImplementation — add cast
- Line ~244 (double start test): the mockImplementation returns `{ textStream:..., usage:... }` — add cast
- Line ~267 (resume test): same pattern — add cast
- Line ~312 (token counts test): `makeMockStream` is not used; instead variables `customStream` and `customUsage` build the object — cast the object `{ textStream: customStream, usage: customUsage } as unknown as ReturnType<typeof streamLLM>`

In `tests/conversation/manager-sse.test.ts`:
- Line ~201 (stop emits idle test): inline `{ textStream:..., usage:... }` — add cast
- Line ~226 (system error test): inline `{ textStream:..., usage:... }` — add cast

**Fix C: agent.name access in manager.test.ts**

At line 461 in `tests/conversation/manager.test.ts`, change:
```typescript
streamCallOrder.push(`buildContext:${agent.name}`);
```
To:
```typescript
streamCallOrder.push(`buildContext:${(agent as any).name}`);
```

After all fixes, run `npx tsc --noEmit` to confirm zero errors across all files, then run `npm test` to confirm all tests still pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as unknown as ReturnType<typeof streamLLM>" tests/conversation/manager.test.ts` returns at least 5 (makeMockStream + inline mocks)
    - `grep -c "as unknown as ReturnType<typeof streamLLM>" tests/conversation/manager-sse.test.ts` returns at least 3 (makeMockStream + 2 inline mocks)
    - `grep -c "(agent as any).name" tests/conversation/manager.test.ts` returns 1
    - `npx tsc --noEmit` exits with code 0 (zero errors across ALL files)
    - `npm test` exits with code 0
  </acceptance_criteria>
  <done>tsc --noEmit reports zero errors, all tests pass, no production code modified, casts applied only at mock boundaries</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (zero errors — the primary DEBT-02 gate)
- `npm test` passes (no test regressions)
- `grep -rn "as any" src/` returns no new matches (production code untouched)
</verification>

<success_criteria>
- `npx tsc --noEmit` reports zero errors across all test files
- No `as any` casts added to any file under `src/` (only `tests/`)
- All existing tests continue to pass
- Mock return values are typed via `as unknown as ReturnType<typeof streamLLM>` pattern
</success_criteria>

<output>
After completion, create `.planning/phases/11-tech-debt-cleanup/11-02-SUMMARY.md`
</output>
