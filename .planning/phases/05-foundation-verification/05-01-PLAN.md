---
phase: 05-foundation-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-foundation-verification/05-VERIFICATION.md
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-04
  - AGNT-01
  - AGNT-02
  - AGNT-03

must_haves:
  truths:
    - "VERIFICATION.md exists in the phase directory with status: passed"
    - "All 7 requirements (ROOM-01..04, AGNT-01..03) have VERIFIED status with file+line evidence"
    - "Test suite output is captured showing all 121+ tests passing"
    - "01-UAT.md results are cross-referenced for each requirement"
    - "Anti-pattern scan results (TODO/FIXME/HACK) are documented"
  artifacts:
    - path: ".planning/phases/05-foundation-verification/05-VERIFICATION.md"
      provides: "Formal verification report for Phase 1 requirements"
      contains: "status: passed"
  key_links:
    - from: "05-VERIFICATION.md"
      to: "src/db/schema.ts, src/app/api/rooms/route.ts, src/app/api/agents/route.ts"
      via: "evidence citations with file paths and line numbers"
      pattern: "src/.*lines \\d+"
---

<objective>
Write the formal VERIFICATION.md for Phase 1 (Foundation) requirements, proving that ROOM-01 through ROOM-04 and AGNT-01 through AGNT-03 are satisfied by existing code.

Purpose: The milestone audit found all 7 Phase 1 requirements implemented correctly but marked "partial" solely because no VERIFICATION.md exists. This plan closes that gap by auditing existing source files, running the existing test suite, and producing a structured verification document.

Output: `.planning/phases/05-foundation-verification/05-VERIFICATION.md`
</objective>

<execution_context>
@/home/vsman/agents-room/.claude/get-shit-done/workflows/execute-plan.md
@/home/vsman/agents-room/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation-verification/05-RESEARCH.md

<interfaces>
<!-- No code interfaces needed — this is a verification-only plan. -->
<!-- The executor reads source files for evidence and writes a markdown document. -->

Canonical format reference:
`.planning/phases/02-conversation-engine/02-VERIFICATION.md` — Follow this structure exactly.

Evidence sources (read for line-number citations):
- `src/db/schema.ts` — rooms, agents, roomAgents, messages, providerKeys tables
- `src/app/api/rooms/route.ts` — GET+POST room handlers
- `src/app/api/rooms/[roomId]/route.ts` — GET+DELETE room handlers
- `src/app/api/rooms/[roomId]/agents/route.ts` — POST+DELETE copy-on-assign handlers
- `src/app/api/agents/route.ts` — GET+POST agent handlers
- `src/app/api/providers/route.ts` — GET providers with masked apiKey
- `src/app/api/providers/[provider]/route.ts` — PUT upsert API key
- `src/app/api/providers/[provider]/test/route.ts` — POST test connection
- `src/lib/validations.ts` — createRoomSchema, createAgentSchema
- `src/lib/llm/providers.ts` — getModel registry
- `src/lib/llm/gateway.ts` — streamLLM, generateLLM
- `src/components/rooms/RoomWizard.tsx` — 3-step wizard
- `src/components/agents/AgentForm.tsx` — structured prompt fields
- `src/components/layout/Sidebar.tsx` — room list rendering
- `src/components/layout/RoomListItem.tsx` — status dot, delete button
- `src/components/settings/ProviderCard.tsx` — status indicators, test connection
- `src/stores/roomStore.ts` — fetchRooms
- `tests/db/rooms.test.ts` — 4 room DB tests
- `tests/db/agents.test.ts` — 2 agent DB tests
- `tests/db/roomAgents.test.ts` — 3 roomAgent tests
- `tests/api/rooms.test.ts` — room API validation tests
- `tests/llm/gateway.test.ts` — 16 gateway tests
- `.planning/phases/01-foundation/01-UAT.md` — 12/12 UAT tests PASSED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 1 source files, run test suite, and write VERIFICATION.md</name>
  <files>.planning/phases/05-foundation-verification/05-VERIFICATION.md</files>

  <read_first>
    .planning/phases/02-conversation-engine/02-VERIFICATION.md
    .planning/phases/01-foundation/01-UAT.md
    .planning/phases/05-foundation-verification/05-RESEARCH.md
    src/db/schema.ts
    src/app/api/rooms/route.ts
    src/app/api/rooms/[roomId]/route.ts
    src/app/api/rooms/[roomId]/agents/route.ts
    src/app/api/agents/route.ts
    src/app/api/providers/route.ts
    src/app/api/providers/[provider]/route.ts
    src/app/api/providers/[provider]/test/route.ts
    src/lib/validations.ts
    src/lib/llm/providers.ts
    src/lib/llm/gateway.ts
    src/components/rooms/RoomWizard.tsx
    src/components/agents/AgentForm.tsx
    src/components/layout/Sidebar.tsx
    src/components/layout/RoomListItem.tsx
    src/components/settings/ProviderCard.tsx
    src/stores/roomStore.ts
    tests/db/rooms.test.ts
    tests/db/agents.test.ts
    tests/db/roomAgents.test.ts
    tests/api/rooms.test.ts
    tests/llm/gateway.test.ts
  </read_first>

  <action>
    **Step 1: Run the full test suite and capture output.**
    Execute `npx vitest run --reporter=verbose 2>&1` and save the output. Confirm all 121+ tests pass. If any fail, document the failure (do NOT fix code — this phase writes no application code).

    **Step 2: Scan for anti-patterns in Phase 1 source files.**
    Run `grep -rn "TODO\|FIXME\|HACK\|PLACEHOLDER\|STUB" src/db/schema.ts src/app/api/rooms/ src/app/api/agents/ src/app/api/providers/ src/lib/validations.ts src/lib/llm/ src/components/rooms/ src/components/agents/ src/components/layout/ src/components/settings/ src/stores/roomStore.ts 2>/dev/null` and record findings.

    **Step 3: Read every source file listed in read_first (above) and note actual line numbers for evidence.**
    For each of the 7 requirements, record:
    - Schema evidence: table name, column names, line numbers in `src/db/schema.ts`
    - API evidence: route path, HTTP method, handler location, line numbers
    - UI evidence: component name, relevant props/state, line numbers
    - Test evidence: test file, test name, pass status
    - UAT evidence: UAT test number and result from `01-UAT.md`

    **Step 4: Write `05-VERIFICATION.md` following the Phase 2 canonical format exactly.**

    The document MUST have this structure:

    ```
    ---
    phase: 05-foundation-verification
    verified: [current ISO timestamp]
    status: passed (or failed if any requirement unverifiable)
    score: 7/7 requirements verified
    re_verification: false
    ---

    # Phase 05: Foundation Verification — Verification Report

    **Phase Goal:** Formally verify all Phase 1 requirements — ROOM-01..04 and AGNT-01..03
    **Verified:** [timestamp]
    **Status:** PASSED
    **Re-verification:** No — initial verification (closing audit gap)

    ---

    ## Goal Achievement

    ### Observable Truths

    One table per requirement with columns: # | Truth | Status | Evidence
    Evidence MUST cite exact file path and line numbers, e.g.:
    `src/db/schema.ts lines 28-45: rooms table with name (notNull), topic (nullable)`

    #### ROOM-01 — Create room with name and optional topic
    [table with schema, API, UI, test, UAT evidence rows]

    #### ROOM-02 — View list of all rooms with status
    [table]

    #### ROOM-03 — Delete room and cascade conversation history
    [table]

    #### ROOM-04 — Open room and see full conversation history
    [table]

    #### AGNT-01 — Create agent with name, persona/role, system prompt
    [table]

    #### AGNT-02 — Assign LLM provider and model to each agent
    [table]

    #### AGNT-03 — Add/remove agents from a room
    [table]

    ---

    ## Required Artifacts

    Table with columns: Artifact | Expected | Status | Details
    List every key file from Phase 1 implementation.

    ---

    ## Key Link Verification

    Table with columns: From | To | Via | Status | Details
    Cover the critical wiring:
    - RoomWizard → POST /api/rooms → db.insert(rooms)
    - AgentForm → POST /api/agents → db.insert(agents)
    - RoomWizard Step 2 → POST /api/rooms/:roomId/agents → copy-on-assign insert
    - Sidebar → GET /api/rooms → roomStore.fetchRooms
    - ProviderCard → PUT /api/providers/:provider → db upsert providerKeys
    - ProviderCard Test → POST /api/providers/:provider/test → generateLLM
    - Room page → GET /api/rooms/:roomId → room with messages+agents

    ---

    ## Requirements Coverage

    Table: Requirement | Source Plan(s) | Description | Status | Evidence
    All 7 requirements with plan references (01-01 through 01-04).

    ---

    ## Anti-Patterns Found

    Report grep results. Format: file:line: match (or "None found" if clean).

    ---

    ## Test Results

    Paste the vitest run output (truncated to key summary lines + any failures).
    Include total test count, pass count, file count.

    ---

    ## Human Verification Required

    Cross-reference all 12 UAT tests from 01-UAT.md:
    Table: UAT Test | Result | Requirement(s) Covered
    Map each UAT test to the requirement(s) it verifies.

    ---

    ## Gaps Summary

    List any gaps found. Expected: "No gaps — all 7 requirements fully verified."
    If gaps exist, describe them with severity.
    ```

    **CRITICAL: Every evidence citation MUST use actual line numbers from reading the file — never approximate or guess.**
  </action>

  <verify>
    <automated>test -f .planning/phases/05-foundation-verification/05-VERIFICATION.md && grep -c "VERIFIED" .planning/phases/05-foundation-verification/05-VERIFICATION.md | xargs test 7 -le</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/05-foundation-verification/05-VERIFICATION.md` exists
    - File contains `status: passed` in frontmatter
    - File contains `score: 7/7` in frontmatter
    - File contains `re_verification: false` in frontmatter
    - File contains exactly 7 requirement sections: `#### ROOM-01`, `#### ROOM-02`, `#### ROOM-03`, `#### ROOM-04`, `#### AGNT-01`, `#### AGNT-02`, `#### AGNT-03`
    - Each requirement section contains at least one row with `VERIFIED` status
    - File contains `## Anti-Patterns Found` section with grep scan results
    - File contains `## Test Results` section with vitest output showing 121+ tests
    - File contains `## Human Verification Required` section referencing 01-UAT.md tests
    - File contains `## Gaps Summary` section
    - File contains `## Key Link Verification` section with at least 5 wiring entries
    - All evidence citations use format `src/path/file.ts lines X-Y:` with real line numbers (no "around line" approximations)
  </acceptance_criteria>

  <done>
    VERIFICATION.md exists with status: passed, score 7/7, all 7 requirements have VERIFIED evidence with exact file paths and line numbers, test suite output captured, UAT cross-referenced, anti-pattern scan documented, and no gaps found.
  </done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/05-foundation-verification/05-VERIFICATION.md` — file exists
2. `grep "status: passed" .planning/phases/05-foundation-verification/05-VERIFICATION.md` — passed status
3. `grep "score: 7/7" .planning/phases/05-foundation-verification/05-VERIFICATION.md` — all requirements covered
4. `grep -c "#### ROOM-\|#### AGNT-" .planning/phases/05-foundation-verification/05-VERIFICATION.md` — returns 7
5. `npx vitest run` — all tests still pass (no regressions from file reads)
</verification>

<success_criteria>
- 05-VERIFICATION.md formally verifies all 7 Phase 1 requirements with concrete evidence
- Document follows the Phase 2 canonical format exactly
- Every requirement cites specific files and line numbers
- Test suite results are captured and documented
- UAT results are cross-referenced
- Anti-pattern scan is completed and documented
- No new application code was written
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-verification/05-01-SUMMARY.md`
</output>
