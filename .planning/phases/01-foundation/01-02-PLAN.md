---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/llm/providers.ts
  - src/lib/llm/gateway.ts
  - tests/llm/gateway.test.ts
  - scripts/test-providers.ts
autonomous: true
requirements:
  - AGNT-02

must_haves:
  truths:
    - "getModel() returns a valid LanguageModel for all 5 providers when given correct config"
    - "streamLLM() streams text chunks from a provider"
    - "generateLLM() returns a complete text string from a provider"
    - "Provider factories receive apiKey from config parameter — never from env vars"
    - "Unknown provider name throws a descriptive error"
  artifacts:
    - path: "src/lib/llm/providers.ts"
      provides: "Provider factory registry — getModel() function and ProviderName type"
      exports: ["getModel", "ProviderName", "ProviderConfig"]
    - path: "src/lib/llm/gateway.ts"
      provides: "Unified LLM interface — streamLLM() and generateLLM() functions"
      exports: ["streamLLM", "generateLLM", "LLMRequest"]
    - path: "tests/llm/gateway.test.ts"
      provides: "Unit tests for provider registry and gateway functions"
      min_lines: 40
    - path: "scripts/test-providers.ts"
      provides: "CLI script for manual integration test of all 5 providers"
      min_lines: 30
  key_links:
    - from: "src/lib/llm/gateway.ts"
      to: "src/lib/llm/providers.ts"
      via: "import { getModel }"
      pattern: "import.*getModel.*from.*providers"
    - from: "src/lib/llm/gateway.ts"
      to: "ai"
      via: "import { streamText, generateText }"
      pattern: "import.*streamText.*from 'ai'"
---

<objective>
Build the LLM gateway — a unified streaming interface over 5 providers (Anthropic, OpenAI, Google, OpenRouter, Ollama) using Vercel AI SDK v6 provider factories.

Purpose: Abstracts all provider differences behind a single `streamLLM()` / `generateLLM()` interface. Every future conversation turn uses this gateway — it must be correct and tested before any API routes or UI.
Output: Provider registry, gateway module, unit tests, and a CLI integration test script.
</objective>

<execution_context>
@/home/vsman/agents-room/.claude/get-shit-done/workflows/execute-plan.md
@/home/vsman/agents-room/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement provider factory registry and LLM gateway</name>
  <files>src/lib/llm/providers.ts, src/lib/llm/gateway.ts, tests/llm/gateway.test.ts</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: LLM Gateway, Pattern 4: Provider Connection Test, Anti-Patterns, Pitfall 2: env var fallback)
  </read_first>
  <behavior>
    - "getModel returns LanguageModel for anthropic" — call getModel('anthropic', 'claude-3-haiku-20240307', { apiKey: 'test-key' }), verify result has type LanguageModel (duck-type check: has `doGenerate` or similar method)
    - "getModel returns LanguageModel for openai" — call getModel('openai', 'gpt-4o-mini', { apiKey: 'test-key' })
    - "getModel returns LanguageModel for google" — call getModel('google', 'gemini-2.0-flash', { apiKey: 'test-key' })
    - "getModel returns LanguageModel for openrouter" — call getModel('openrouter', 'meta-llama/llama-3.1-8b-instruct:free', { apiKey: 'test-key' })
    - "getModel returns LanguageModel for ollama" — call getModel('ollama', 'llama3.2', {}), no apiKey needed
    - "getModel throws for unknown provider" — call getModel('unknown' as any, 'model', {}), expect Error with message containing "Unknown provider"
    - "streamLLM calls streamText with correct parameters" — mock `streamText` from 'ai', call streamLLM with full LLMRequest, verify streamText was called with model, system, messages, temperature, abortSignal
    - "generateLLM calls generateText with correct parameters" — mock `generateText` from 'ai', call generateLLM with full LLMRequest, verify generateText was called correctly
    - "apiKey is passed explicitly to factory — not read from env" — verify that createAnthropic is called with { apiKey: 'explicit-key' } (use vi.mock to spy on factory calls)
  </behavior>
  <action>
1. Create `src/lib/llm/providers.ts` with the EXACT implementation from RESEARCH.md Pattern 3:
   - Export `ProviderName` type: `'anthropic' | 'openai' | 'google' | 'openrouter' | 'ollama'`
   - Export `ProviderConfig` interface: `{ apiKey?: string; baseUrl?: string }`
   - Export `getModel(provider: ProviderName, model: string, config: ProviderConfig): LanguageModel`
   - Switch statement with all 5 providers:
     - `anthropic`: `createAnthropic({ apiKey: config.apiKey! })(model)`
     - `openai`: `createOpenAI({ apiKey: config.apiKey! })(model)`
     - `google`: `createGoogleGenerativeAI({ apiKey: config.apiKey! })(model)`
     - `openrouter`: `createOpenRouter({ apiKey: config.apiKey! }).chat(model)`
     - `ollama`: `createOllama({ baseURL: config.baseUrl ?? 'http://localhost:11434/api' })(model)`
   - Default case throws `new Error(`Unknown provider: ${provider}`)`

2. Create `src/lib/llm/gateway.ts` with the EXACT implementation from RESEARCH.md Pattern 3:
   - Export `LLMRequest` interface with fields: provider (ProviderName), model (string), config (ProviderConfig), system (optional string), messages (Array<{ role: 'user' | 'assistant'; content: string }>), temperature (optional number), abortSignal (optional AbortSignal)
   - Export `streamLLM(request: LLMRequest)` — calls `streamText()` from 'ai' with model from getModel(), system, messages, temperature defaulting to 0.7, abortSignal
   - Export `generateLLM(request: LLMRequest): Promise<string>` — calls `generateText()` from 'ai', returns `result.text`

3. Create `tests/llm/gateway.test.ts` with all behaviors listed above. Use `vi.mock` to mock the AI SDK provider factories and `streamText`/`generateText`. Tests must NOT make real API calls.

4. Run `npx vitest run tests/llm/ --reporter=verbose` — all tests pass.
  </action>
  <verify>
    <automated>npx vitest run tests/llm/ --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/llm/providers.ts contains "export type ProviderName", "export interface ProviderConfig", "export function getModel"
    - src/lib/llm/providers.ts contains "createAnthropic", "createOpenAI", "createGoogleGenerativeAI", "createOpenRouter", "createOllama"
    - src/lib/llm/providers.ts contains "apiKey: config.apiKey!" for anthropic, openai, google, openrouter cases
    - src/lib/llm/providers.ts contains "Unknown provider" in error message
    - src/lib/llm/gateway.ts contains "export function streamLLM", "export async function generateLLM", "export interface LLMRequest"
    - src/lib/llm/gateway.ts imports "streamText" and "generateText" from 'ai'
    - tests/llm/gateway.test.ts contains "vi.mock" and tests for all 5 provider names
    - `npx vitest run tests/llm/` exits 0
  </acceptance_criteria>
  <done>Provider factory registry handles all 5 providers. Gateway provides streamLLM() and generateLLM() with explicit apiKey passing. All unit tests pass with mocked AI SDK.</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI integration test script for live provider verification</name>
  <files>scripts/test-providers.ts</files>
  <read_first>
    src/lib/llm/providers.ts (the getModel function and ProviderName type just created)
    src/lib/llm/gateway.ts (the generateLLM function just created)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Provider Connection Test — DEFAULT_TEST_MODELS)
  </read_first>
  <action>
1. Create `scripts/test-providers.ts` — a standalone CLI script that:
   - Reads API keys from environment variables (ANTHROPIC_API_KEY, OPENAI_API_KEY, GOOGLE_API_KEY, OPENROUTER_API_KEY) for this script only (production code reads from DB)
   - For each provider where a key is available, calls `generateLLM()` with:
     - anthropic: model 'claude-3-haiku-20240307'
     - openai: model 'gpt-4o-mini'
     - google: model 'gemini-2.0-flash'
     - openrouter: model 'meta-llama/llama-3.1-8b-instruct:free'
     - ollama: model 'llama3.2' (no key needed, tests local server)
   - Message: `[{ role: 'user', content: 'Reply with exactly one word: hello' }]`
   - Prints result for each provider: `[provider] OK: {response text}` or `[provider] FAIL: {error message}`
   - Skips providers without keys (prints `[provider] SKIPPED: no API key`)
   - Add a package.json script: `"test:providers": "npx tsx scripts/test-providers.ts"`

2. Install tsx as a dev dependency: `npm install -D tsx`

3. This script is for manual verification only — it is NOT run in CI or automated tests.
  </action>
  <verify>
    <automated>npx tsx scripts/test-providers.ts 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - scripts/test-providers.ts contains "generateLLM" import
    - scripts/test-providers.ts contains "claude-3-haiku-20240307", "gpt-4o-mini", "gemini-2.0-flash", "meta-llama/llama-3.1-8b-instruct:free", "llama3.2"
    - scripts/test-providers.ts contains "SKIPPED" handling for missing keys
    - package.json contains "test:providers" script
    - `npx tsx scripts/test-providers.ts` exits without crash (providers will show SKIPPED if no keys set)
  </acceptance_criteria>
  <done>CLI integration test script exists for manual verification of all 5 LLM providers. Script can be run with env vars to confirm real provider connectivity.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/llm/ --reporter=verbose` — all gateway tests pass
- `npx tsx scripts/test-providers.ts` runs without crashing (shows SKIPPED for unconfigured providers)
- providers.ts exports getModel with all 5 provider factories
- gateway.ts exports streamLLM and generateLLM with unified LLMRequest interface
- No env var fallback in production code — apiKey always passed explicitly from config parameter
</verification>

<success_criteria>
- LLM gateway provides unified streaming and non-streaming interface across 5 providers
- All unit tests pass with mocked AI SDK (no real API calls in tests)
- CLI script available for manual provider verification
- Gateway ready for use by API routes in Plan 01-03
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
